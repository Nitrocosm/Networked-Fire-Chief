{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Pure wire types + role-filtering. Runs on both server and client, so it
    // stays DOM-free and node-free.
    "lib": ["ES2022"],
    "types": []
  },
  "include": ["src", "test"]
}
