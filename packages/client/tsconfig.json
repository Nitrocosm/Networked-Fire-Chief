{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // The only package allowed to touch the DOM.
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": []
  },
  "include": ["src", "test"]
}
